DROP TABLE IF EXISTS User;

CREATE TABLE User (
    id INT AUTO_INCREMENT,
    username VARCHAR(255),
    email VARCHAR(255),
    tel_no INT,
    password VARCHAR(255),
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY(id)
);

CREATE TABLE pre_Owned_Item_Ad (
    p_id INT AUTO_INCREMENT,
    item_name VARCHAR(255),
    price DOUBLE,
    category VARCHAR(255),
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(255),
    PRIMARY KEY(p_id),
    FOREIGN KEY (seller_id) REFERENCES User(id) ON DELETE CASCADE
);

INSERT INTO Users (name, age) VALUES ('Nihara', '20');
INSERT INTO Users (name, age) VALUES ('Inuka', '21');
INSERT INTO Users (name, age) VALUES ('Nethmi', '22');
INSERT INTO Users (name, age) VALUES ('Rusiru', '23');
